//! Game setup and initialization functions

use super::Lobby;

impl Lobby {
    // Game setup helper functions can be added here
    // The expand_zone_metadata, expand_zone_groups, and get_current_phase_prompt
    // methods are already defined in the main Lobby impl in lobby.rs
}