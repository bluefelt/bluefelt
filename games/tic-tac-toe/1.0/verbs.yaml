- id: placeMark
  uses: grid.move
  with:
    source: marks_{actor}
    target:
      zone: board
  then:
    - action: advanceTurn
    - action: checkWin
    - action: checkTie

- id: advanceTurn
  uses: turn.advance
  auto: true

- id: checkWin
  auto: true
  conditions:
    - type: consecutiveMarksInRow
      params:
        zone: board
        count: 3
        entity: mark_{actor}
      result: gameWin

- id: checkTie
  auto: true
  conditions:
    - type: allCellsFilled
      params:
        zone: board
      result: gameTie

- id: resetGame
  uses: zone.reset
  with:
    zones: [board]
  then:
    - action: turn.reset