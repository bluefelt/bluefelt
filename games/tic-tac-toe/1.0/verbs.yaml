- id: place
  builtin: moveEntity
  params:
    source: marks_{actor}
    target:
      zone: board
      constraints:
        vacant: true
  ui:
    direction: "Choose a cell to place a mark"
  triggers:
    - checkGameEnd

- id: checkGameEnd
  hook: checkGameEnd
  auto: true